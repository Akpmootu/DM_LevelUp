rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Basic test rules since no auth is implemented
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
